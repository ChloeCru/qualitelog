name: Github Action Tests

on:
  push:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run JUnit testAddition
        run: mvn test -Dtest=AppTest#testAddition
